# Environment / secrets
.env
*.env

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store
~$*

# Excel lock files
~$*.xlsx

# scratch / temp
scratch_*
scratchpad/
*.log
logo_temp_*.png

# the deployed image ships the exam (Dockerfile COPY . .), so track the exam
# manifest + figures; ignore only the transient checkpoint/workbook.
questins_visual/full_exam_checkpoint.jsonl
results/

# duplicated / archive dirs
Distractor copy/
archive/
